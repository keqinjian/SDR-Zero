#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
tx_radio2_tunes.py —— f3 的射频档位选择（不依赖 GNU Radio / ROS）
================================================================

【这个文件是干什么的】
f3 由 tx_radio2.py（收音机）和 info_decoder_f3.py（协议解析）两个进程组成，
两边都要知道"现在用哪个档"。这个数字放在这里，两个文件都从这里读，
改一处就够了，不需要设置任何环境变量。

【档位表在哪】
具体的带宽/增益/滤波器数值在 tx_radio2.py 的 ANTIJAM_PROFILES 里。
"""

from __future__ import annotations

# =============================================================================
# ★★★★★ 调参面板：现场优先只改这一行 ★★★★★
# =============================================================================
# 可选档位（数值见 tx_radio2.py 的 ANTIJAM_PROFILES）：
#   "baseline" 等同最早的 tx_radio.py，用来确认 f3 没有引入性能回退
#   "balanced" 首选抗干扰档：先缩模拟带宽，再用陡峭 FIR 压制二/三级邻道
#   "strong"   强干扰实验档：带宽和增益更激进，无干扰时也必须复测 10Hz
ANTIJAM_PROFILE = "balanced"
# =============================================================================
