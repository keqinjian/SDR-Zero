#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
rm_info_mock_v2.py
==================
模拟官方「信息波」发射端（协议 V2.0.0 / 规则 V2.1.0），
直接喂当前 info_decoder_f1（无需 Pluto / GRC）。

---------------------------------------------------------------------------
与旧 rm_referee_mock.py 的区别
---------------------------------------------------------------------------
  旧版：往 12345 发「空口整包字节」，供 GRC 的 UDP Source→GFSK Mod→TX。
  本版：往 14346 发「每字节一个 bit（数值 0/1）」，格式与 GFSK Demod→UDP Sink 一致。

用法（Ubuntu + ROS2）：
  1) RM_ENABLE_GRC_WATCHDOG=0 python3 competition/info/info_decoder_f1.py
  2) python3 rm_info_mock_v2.py
  3) 看 decoder 日志里 0x0A01～0x0A05 计数是否上涨
     （务必关看门狗，否则 GRC 与 mock 抢同一 UDP 口）
"""

from __future__ import annotations

import argparse
import socket
import struct
import time

# =============================================================================
# 配置（一般只改这几项）
# =============================================================================

UDP_IP = "127.0.0.1"
UDP_PORT = 14346          # = info_decoder_f1.UDP_PORT
ROUND_HZ = 10.0           # 官方约 10 Hz 一轮业务
INVERT_POLARITY = False   # True：整流转反相，测 decoder 极性双搜
BITS_PER_UDP = 512        # 单次 UDP 携带的 bit 数（勿过大）

ACCESS_CODE = bytes.fromhex("2F6F4C74B914492E")
HEADER = bytes([0x00, 0x0F, 0x00, 0x0F])
AIR_PAYLOAD_LEN = 15
LEN_0A05 = 41

# =============================================================================
# CRC（与裁判系统 / decoder 一致）
# =============================================================================

CRC8_TAB = [
    ,
]

CRC16_TAB = [
    0x0000, 0x1189, 0x2312, 0x329b, 0x4624, 0x57ad, 0x6536, 0x74bf, 0x8c48, 0x9dc1, 0xaf5a, 0xbed3,
    0xca6c, 0xdbe5, 0xe97e, 0xf8f7, 0x1081, 0x0108, 0x3393, 0x221a, 0x56a5, 0x472c, 0x75b7, 0x643e,
    0x9cc9, 0x8d40, 0xbfdb, 0xae52, 0xdaed, 0xcb64, 0xf9ff, 0xe876, 0x2102, 0x308b, 0x0210, 0x1399,
    0x6726, 0x76af, 0x4434, 0x55bd, 0xad4a, 0xbcc3, 0x8e58, 0x9fd1, 0xeb6e, 0xfae7, 0xc87c, 0xd9f5,
    0x3183, 0x200a, 0x1291, 0x0318, 0x77a7, 0x662e, 0x54b5, 0x453c, 0xbdcb, 0xac42, 0x9ed9, 0x8f50,
    0xfbef, 0xea66, 0xd8fd, 0xc974, 0x4204, 0x538d, 0x6116, 0x709f, 0x0420, 0x15a9, 0x2732, 0x36bb,
    0xce4c, 0xdfc5, 0xed5e, 0xfcd7, 0x8868, 0x99e1, 0xab7a, 0xbaf3, 0x5285, 0x430c, 0x7197, 0x601e,
    0x14a1, 0x0528, 0x37b3, 0x263a, 0xdecd, 0xcf44, 0xfddf, 0xec56, 0x98e9, 0x8960, 0xbbfb, 0xaa72,
    0x6306, 0x728f, 0x4014, 0x519d, 0x2522, 0x34ab, 0x0630, 0x17b9, 0xef4e, 0xfec7, 0xcc5c, 0xddd5,
    0xa96a, 0xb8e3, 0x8a78, 0x9bf1, 0x7387, 0x620e, 0x5095, 0x411c, 0x35a3, 0x242a, 0x16b1, 0x0738,
    0xffcf, 0xee46, 0xdcdd, 0xcd54, 0xb9eb, 0xa862, 0x9af9, 0x8b70, 0x8408, 0x9581, 0xa71a, 0xb693,
    0xc22c, 0xd3a5, 0xe13e, 0xf0b7, 0x0840, 0x19c9, 0x2b52, 0x3adb, 0x4e64, 0x5fed, 0x6d76, 0x7cff,
    0x9489, 0x8500, 0xb79b, 0xa612, 0xd2ad, 0xc324, 0xf1bf, 0xe036, 0x18c1, 0x0948, 0x3bd3, 0x2a5a,
    0x5ee5, 0x4f6c, 0x7df7, 0x6c7e, 0xa50a, 0xb483, 0x8618, 0x9791, 0xe32e, 0xf2a7, 0xc03c, 0xd1b5,
    0x2942, 0x38cb, 0x0a50, 0x1bd9, 0x6f66, 0x7eef, 0x4c74, 0x5dfd, 0xb58b, 0xa402, 0x9699, 0x8710,
    0xf3af, 0xe226, 0xd0bd, 0xc134, 0x39c3, 0x284a, 0x1ad1, 0x0b58, 0x7fe7, 0x6e6e, 0x5cf5, 0x4d7c,
    0xc60c, 0xd785, 0xe51e, 0xf497, 0x8028, 0x91a1, 0xa33a, 0xb2b3, 0x4a44, 0x5bcd, 0x6956, 0x78df,
    0x0c60, 0x1de9, 0x2f72, 0x3efb, 0xd68d, 0xc704, 0xf59f, 0xe416, 0x90a9, 0x8120, 0xb3bb, 0xa232,
    0x5ac5, 0x4b4c, 0x79d7, 0x685e, 0x1ce1, 0x0d68, 0x3ff3, 0x2e7a, 0xe70e, 0xf687, 0xc41c, 0xd595,
    0xa12a, 0xb0a3, 0x8238, 0x93b1, 0x6b46, 0x7acf, 0x4854, 0x59dd, 0x2d62, 0x3ceb, 0x0e70, 0x1ff9,
    0xf78f, 0xe606, 0xd49d, 0xc514, 0xb1ab, 0xa022, 0x92b9, 0x8330, 0x7bc7, 0x6a4e, 0x58d5, 0x495c,
    0x3de3, 0x2c6a, 0x1ef1, 0x0f78,
]


def calc_crc8(data: bytes) -> int:
    crc = 0xFF
    for b in data:
        crc = CRC8_TAB[crc ^ b]
    return crc


def calc_crc16(data: bytes) -> int:
    crc = 0xFFFF
    for b in data:
        crc = ((crc >> 8) ^ CRC16_TAB[(crc ^ b) & 0xFF]) & 0xFFFF
    return crc


def build_referee_frame(cmd_id: int, payload: bytes, seq: int) -> bytes:
    header = bytearray(5)
    header[0] = 0xA5
    struct.pack_into("<H", header, 1, len(payload))
    header[3] = seq & 0xFF
    header[4] = calc_crc8(header[:4])
    body = header + struct.pack("<H", cmd_id) + payload
    frame = bytearray(body + b"\x00\x00")
    struct.pack_into("<H", frame, len(frame) - 2, calc_crc16(frame[:-2]))
    return bytes(frame)


def bytes_to_bits(data: bytes) -> str:
    return "".join(f"{b:08b}" for b in data)


def stream_to_demod_bytes(serial_stream: bytes, invert: bool) -> bytes:
    """
    裁判字节流 → 空口切片 → MSB-first 比特 → UDP 载荷（每字节 0 或 1）。
    不足 15 的倍数时右侧补 0（与官方空口切片习惯一致）。
    """
    pad = (-len(serial_stream)) % AIR_PAYLOAD_LEN
    if pad:
        serial_stream = serial_stream + bytes(pad)

    bits: list[str] = []
    for i in range(0, len(serial_stream), AIR_PAYLOAD_LEN):
        packet = ACCESS_CODE + HEADER + serial_stream[i:i + AIR_PAYLOAD_LEN]
        bits.append(bytes_to_bits(packet))
    bit_str = "".join(bits)
    if invert:
        bit_str = "".join("1" if c == "0" else "0" for c in bit_str)
    return bytes(1 if c == "1" else 0 for c in bit_str)


def build_unit_buff(heal: int, cool: int, defense: int, neg_def: int, attack: int) -> bytes:
    return struct.pack("<BHBBH", heal, cool, defense, neg_def, attack)


def build_info_round(seq_start: int, hero_hp: int) -> bytes:
    """
    一轮信息波业务（协议 V2）：0x0A01～0x0A05，合计 140 字节。
    不含 0x0A06（密钥只在干扰波）。
    """
    # 0x0A01 坐标：uint16 cm
    pos = struct.pack(
        "<12H",
        250, 310,   # 英雄 2.50m, 3.10m
        120, 50,    # 工程
        50, 100,    # 步兵3
        120, 0,     # 步兵4
        1000, 500,  # 空中
        0, 400,     # 哨兵
    )
    # 0x0A02 血量：英雄/工程/步3/步4/保留/哨兵
    hp = struct.pack("<6H", hero_hp, 500, 800, 800, 0, 600)
    # 0x0A03 发弹量
    ammo = struct.pack("<5H", 150, 450, 400, 500, 800)
    # 0x0A04 宏观：剩余/累计金币 + 状态位（bit8=基地增益 → 256）
    macro = struct.pack("<HHI", 350, 1200, 256)
    # 0x0A05：5×7 增益 + 姿态 + 5 字节主要状态 = 41B
    buff = (
        build_unit_buff(10, 100, 20, 0, 150)
        + build_unit_buff(0, 0, 0, 0, 100)
        + build_unit_buff(0, 0, 0, 30, 100)
        + build_unit_buff(0, 0, 0, 0, 100)
        + build_unit_buff(5, 50, 40, 0, 120)
        + bytes([2])                          # 哨兵姿态：防御
        + bytes([0, 0, 0, 0, 0])              # 主要状态：全存活
    )
    assert len(buff) == LEN_0A05, f"0x0A05 len={len(buff)}, want {LEN_0A05}"

    frames = [
        build_referee_frame(0x0A01, pos, seq_start + 0),
        build_referee_frame(0x0A02, hp, seq_start + 1),
        build_referee_frame(0x0A03, ammo, seq_start + 2),
        build_referee_frame(0x0A04, macro, seq_start + 3),
        build_referee_frame(0x0A05, buff, seq_start + 4),
    ]
    stream = b"".join(frames)
    assert len(stream) == 140, f"info round must be 140B, got {len(stream)}"
    return stream


def send_demod_bits(sock: socket.socket, target: tuple, demod: bytes) -> None:
    for i in range(0, len(demod), BITS_PER_UDP):
        sock.sendto(demod[i:i + BITS_PER_UDP], target)


def main() -> None:
    parser = argparse.ArgumentParser(description="信息波 V2 mock → info_decoder_f1")
    parser.add_argument("--ip", default=UDP_IP)
    parser.add_argument("--port", type=int, default=UDP_PORT)
    parser.add_argument("--hz", type=float, default=ROUND_HZ)
    parser.add_argument("--invert", action="store_true", default=INVERT_POLARITY,
                        help="反相比特流，测极性双搜")
    args = parser.parse_args()

    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    target = (args.ip, args.port)
    period = 1.0 / args.hz

    print(
        f"信息波 mock V2 → {args.ip}:{args.port} | "
        f"{args.hz:.1f} Hz | 0x0A05={LEN_0A05}B | 反相={'开' if args.invert else '关'}"
    )
    print("请先启动 info_decoder_f1（建议 ENABLE_GRC_WATCHDOG=False）\n")

    hero_hp = 2000
    seq = 0
    round_n = 0
    try:
        while True:
            t0 = time.time()
            stream = build_info_round(seq, hero_hp)
            seq = (seq + 5) % 256
            demod = stream_to_demod_bytes(stream, invert=args.invert)
            send_demod_bits(sock, target, demod)

            round_n += 1
            if round_n % 10 == 0:
                print(
                    f"[info] round={round_n} hero_hp={hero_hp} "
                    f"serial={len(stream)}B demod_bits={len(demod)}"
                )

            hero_hp -= 15
            if hero_hp <= 0:
                hero_hp = 2000

            # 对齐约 10 Hz；发完再睡剩余时间
            elapsed = time.time() - t0
            if elapsed < period:
                time.sleep(period - elapsed)
    except KeyboardInterrupt:
        print("\n停止信息波 mock")
    finally:
        sock.close()


if __name__ == "__main__":
    main()
